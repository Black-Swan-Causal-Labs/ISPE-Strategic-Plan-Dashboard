# Build output — regenerated by build_review_site.py on every deploy.
# Never edit dist/index.html by hand: the next build silently overwrites it.
dist/

# Local secrets and local D1 state.
.dev.vars
.wrangler/
node_modules/
